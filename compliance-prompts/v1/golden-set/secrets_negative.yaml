cases:
  - id: secrets-neg-001
    prompt: secrets_api_keys_detector
    input: "Example placeholder: ANTHROPIC_API_KEY=sk-ant-api03-<REDACTED>"
    expected:
      detected: false
  - id: secrets-neg-002
    prompt: secrets_github_pat_detector
    input: "Documentation snippet: store your token as `ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX` (placeholder X×36)"
    expected:
      detected: false
  - id: secrets-neg-003
    prompt: secrets_db_credentials_detector
    input: "Use env var: DATABASE_URL=postgres://${USER}:${PASS}@${HOST}:5432/${DB}"
    expected:
      detected: false
  - id: secrets-neg-004
    prompt: secrets_oauth_tokens_detector
    input: "Authorization: Bearer <YOUR_ACCESS_TOKEN_HERE>"
    expected:
      detected: false
  - id: secrets-neg-005
    prompt: secrets_cf_tokens_detector
    input: "CF setup guide: paste your token from https://dash.cloudflare.com/profile/api-tokens"
    expected:
      detected: false
